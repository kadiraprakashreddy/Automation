import {
  SchedulesCreateActionLabels,
  SchedulesCreatePageContent,
} from './schedules-create.model';

/**
 * Page header content for the schedules create workflow
 */
export const SCHEDULES_CREATE_PAGE_CONTENT: SchedulesCreatePageContent = {
  pageHeader: 'Create a termination rule',
};

/**
 * Action button labels used throughout the schedules create workflow
 */
export const SCHEDULES_CREATE_ACTION_LABELS: SchedulesCreateActionLabels = {
  submit: 'Submit',
  cancel: 'Cancel',
};
